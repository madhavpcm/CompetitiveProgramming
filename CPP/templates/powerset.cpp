
void PowerSet(vector<int64_t> arr ){
    vector<vector<int64_t>> subSets;

    uint64_t N = pow(2, arr.size());

    for (uint64_t i =0 ; i < N ; i++){
        vector<int64_t> subSet;

        for (uint64_t j =0 ; j < arr.size(); j++){
            
        }
    }
}